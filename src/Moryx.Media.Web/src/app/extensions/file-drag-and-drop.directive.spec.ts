import { FileDragAndDropDirective } from './file-drag-and-drop.directive';

describe('FileDragAndDropDirective', () => {
  it('should create an instance', () => {
    const directive = new FileDragAndDropDirective();
    expect(directive).toBeTruthy();
  });
});
