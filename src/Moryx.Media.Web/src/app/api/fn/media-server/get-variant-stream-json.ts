/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface GetVariantStream$Json$Params {
  guid: string;
  variantName: string;
  preview?: boolean;
}

export function getVariantStream$Json(http: HttpClient, rootUrl: string, params: GetVariantStream$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<Blob>> {
  const rb = new RequestBuilder(rootUrl, getVariantStream$Json.PATH, 'get');
  if (params) {
    rb.path('guid', params.guid, {});
    rb.path('variantName', params.variantName, {});
    rb.query('preview', params.preview, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Blob>;
    })
  );
}

getVariantStream$Json.PATH = '/api/moryx/media/{guid}/{variantName}/stream';
