/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface GetVariantStream$Plain$Params {
  guid: string;
  variantName: string;
  preview?: boolean;
}

export function getVariantStream$Plain(http: HttpClient, rootUrl: string, params: GetVariantStream$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Blob>> {
  const rb = new RequestBuilder(rootUrl, getVariantStream$Plain.PATH, 'get');
  if (params) {
    rb.path('guid', params.guid, {});
    rb.path('variantName', params.variantName, {});
    rb.query('preview', params.preview, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Blob>;
    })
  );
}

getVariantStream$Plain.PATH = '/api/moryx/media/{guid}/{variantName}/stream';

