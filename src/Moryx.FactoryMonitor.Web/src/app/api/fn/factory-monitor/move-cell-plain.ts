/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CellLocationModel } from '../../models/cell-location-model';

export interface MoveCell$Plain$Params {
      body?: CellLocationModel
}

export function moveCell$Plain(http: HttpClient, rootUrl: string, params?: MoveCell$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<CellLocationModel>> {
  const rb = new RequestBuilder(rootUrl, moveCell$Plain.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CellLocationModel>;
    })
  );
}

moveCell$Plain.PATH = '/api/moryx/factory-monitor/move-cell';
