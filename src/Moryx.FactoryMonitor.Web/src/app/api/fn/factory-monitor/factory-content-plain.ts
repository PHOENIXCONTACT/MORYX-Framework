/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { VisualizableItemModel } from '../../models/visualizable-item-model';

export interface FactoryContent$Plain$Params {
  factoryId: number;
}

export function factoryContent$Plain(http: HttpClient, rootUrl: string, params: FactoryContent$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<VisualizableItemModel>>> {
  const rb = new RequestBuilder(rootUrl, factoryContent$Plain.PATH, 'get');
  if (params) {
    rb.path('factoryId', params.factoryId, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<VisualizableItemModel>>;
    })
  );
}

factoryContent$Plain.PATH = '/api/moryx/factory-monitor/factory-content/{factoryId}';

