/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CellPropertySettings } from '../../models/cell-property-settings';

export interface GetCellPropertiesSettings$Params {
  identifier: string;
}

export function getCellPropertiesSettings(http: HttpClient, rootUrl: string, params: GetCellPropertiesSettings$Params, context?: HttpContext): Observable<StrictHttpResponse<{
[key: string]: CellPropertySettings;
}>> {
  const rb = new RequestBuilder(rootUrl, getCellPropertiesSettings.PATH, 'get');
  if (params) {
    rb.path('identifier', params.identifier, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      [key: string]: CellPropertySettings;
      }>;
    })
  );
}

getCellPropertiesSettings.PATH = '/api/moryx/factory-monitor/cell-properties/{identifier}';
