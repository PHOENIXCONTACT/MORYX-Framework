/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TransportRouteModel } from '../../models/transport-route-model';

export interface GetRoutes$Plain$Params {
}

export function getRoutes$Plain(http: HttpClient, rootUrl: string, params?: GetRoutes$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TransportRouteModel>>> {
  const rb = new RequestBuilder(rootUrl, getRoutes$Plain.PATH, 'post');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TransportRouteModel>>;
    })
  );
}

getRoutes$Plain.PATH = '/api/moryx/factory-monitor/routes';

