/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FactoryStateModel } from '../../models/factory-state-model';

export interface InitialFactoryState$Params {
}

export function initialFactoryState(http: HttpClient, rootUrl: string, params?: InitialFactoryState$Params, context?: HttpContext): Observable<StrictHttpResponse<FactoryStateModel>> {
  const rb = new RequestBuilder(rootUrl, initialFactoryState.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FactoryStateModel>;
    })
  );
}

initialFactoryState.PATH = '/api/moryx/factory-monitor/state';
