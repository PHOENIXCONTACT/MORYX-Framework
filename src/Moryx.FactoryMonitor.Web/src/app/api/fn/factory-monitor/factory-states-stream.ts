/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OrderChangedModel } from '../../models/order-changed-model';

export interface FactoryStatesStream$Params {
}

export function factoryStatesStream(http: HttpClient, rootUrl: string, params?: FactoryStatesStream$Params, context?: HttpContext): Observable<StrictHttpResponse<OrderChangedModel>> {
  const rb = new RequestBuilder(rootUrl, factoryStatesStream.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OrderChangedModel>;
    })
  );
}

factoryStatesStream.PATH = '/api/moryx/factory-monitor/state-stream';
