/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CellLocationModel } from '../../models/cell-location-model';

export interface MoveCell$Params {
      body?: CellLocationModel
}

export function moveCell(http: HttpClient, rootUrl: string, params?: MoveCell$Params, context?: HttpContext): Observable<StrictHttpResponse<CellLocationModel>> {
  const rb = new RequestBuilder(rootUrl, moveCell.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CellLocationModel>;
    })
  );
}

moveCell.PATH = '/api/moryx/factory-monitor/move-cell';

