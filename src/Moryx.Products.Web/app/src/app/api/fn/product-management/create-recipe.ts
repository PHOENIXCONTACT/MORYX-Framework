/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RecipeModel } from '../../models/recipe-model';

export interface CreateRecipe$Params {
  recipeType: string;
}

export function createRecipe(http: HttpClient, rootUrl: string, params: CreateRecipe$Params, context?: HttpContext): Observable<StrictHttpResponse<RecipeModel>> {
  const rb = new RequestBuilder(rootUrl, createRecipe.PATH, 'get');
  if (params) {
    rb.path('recipeType', params.recipeType, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RecipeModel>;
    })
  );
}

createRecipe.PATH = '/api/moryx/products/recipe/construct/{recipeType}';

