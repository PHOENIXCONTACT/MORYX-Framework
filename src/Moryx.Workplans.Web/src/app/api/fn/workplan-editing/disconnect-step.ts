/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { NodeConnector } from '../../models/node-connector';
import { WorkplanSessionModel } from '../../models/workplan-session-model';

export interface DisconnectStep$Params {
  sessionId: string;
  targetNodeId: number;
  targetIndex: number;
      body?: NodeConnector
}

export function disconnectStep(http: HttpClient, rootUrl: string, params: DisconnectStep$Params, context?: HttpContext): Observable<StrictHttpResponse<WorkplanSessionModel>> {
  const rb = new RequestBuilder(rootUrl, disconnectStep.PATH, 'delete');
  if (params) {
    rb.path('sessionId', params.sessionId, {});
    rb.path('targetNodeId', params.targetNodeId, {});
    rb.path('targetIndex', params.targetIndex, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WorkplanSessionModel>;
    })
  );
}

disconnectStep.PATH = '/api/moryx/workplans/sessions/{sessionId}/nodes/{targetNodeId}/{targetIndex}';

