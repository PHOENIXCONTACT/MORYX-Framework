/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OpenSessionRequest } from '../../models/open-session-request';
import { WorkplanSessionModel } from '../../models/workplan-session-model';

export interface EditWorkplan$Params {
      body?: OpenSessionRequest
}

export function editWorkplan(http: HttpClient, rootUrl: string, params?: EditWorkplan$Params, context?: HttpContext): Observable<StrictHttpResponse<WorkplanSessionModel>> {
  const rb = new RequestBuilder(rootUrl, editWorkplan.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WorkplanSessionModel>;
    })
  );
}

editWorkplan.PATH = '/api/moryx/workplans/sessions';
