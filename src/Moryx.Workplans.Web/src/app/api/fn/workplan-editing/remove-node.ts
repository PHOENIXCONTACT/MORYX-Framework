/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WorkplanSessionModel } from '../../models/workplan-session-model';

export interface RemoveNode$Params {
  sessionId: string;
  nodeId: number;
}

export function removeNode(http: HttpClient, rootUrl: string, params: RemoveNode$Params, context?: HttpContext): Observable<StrictHttpResponse<WorkplanSessionModel>> {
  const rb = new RequestBuilder(rootUrl, removeNode.PATH, 'delete');
  if (params) {
    rb.path('sessionId', params.sessionId, {});
    rb.path('nodeId', params.nodeId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WorkplanSessionModel>;
    })
  );
}

removeNode.PATH = '/api/moryx/workplans/sessions/{sessionId}/nodes/{nodeId}';
