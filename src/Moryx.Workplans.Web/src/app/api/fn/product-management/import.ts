/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Entry } from '@moryx/ngx-web-framework/entry-editor';
import { ProductModel } from '../../models/product-model';

export interface Import$Params {
  importerName: string;
      body?: Entry
}

export function import$(http: HttpClient, rootUrl: string, params: Import$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ProductModel>>> {
  const rb = new RequestBuilder(rootUrl, import$.PATH, 'post');
  if (params) {
    rb.path('importerName', params.importerName, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ProductModel>>;
    })
  );
}

import$.PATH = '/api/moryx/products/importers/{importerName}';
