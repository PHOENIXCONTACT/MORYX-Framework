/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RecipeModel } from '../../models/recipe-model';

export interface GetRecipes$Params {
  id: number;
  classification: number;
}

export function getRecipes(http: HttpClient, rootUrl: string, params: GetRecipes$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<RecipeModel>>> {
  const rb = new RequestBuilder(rootUrl, getRecipes.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
    rb.path('classification', params.classification, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<RecipeModel>>;
    })
  );
}

getRecipes.PATH = '/api/moryx/products/types/{id}/recipes/{classification}';
