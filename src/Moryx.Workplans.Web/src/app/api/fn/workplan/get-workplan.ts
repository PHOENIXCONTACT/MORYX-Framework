/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WorkplanModel } from '../../models/workplan-model';

export interface GetWorkplan$Params {
  id: number;
}

export function getWorkplan(http: HttpClient, rootUrl: string, params: GetWorkplan$Params, context?: HttpContext): Observable<StrictHttpResponse<WorkplanModel>> {
  const rb = new RequestBuilder(rootUrl, getWorkplan.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WorkplanModel>;
    })
  );
}

getWorkplan.PATH = '/api/moryx/workplans/{id}';
