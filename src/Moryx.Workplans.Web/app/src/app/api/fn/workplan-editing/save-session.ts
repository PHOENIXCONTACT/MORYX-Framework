/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WorkplanSessionModel } from '../../models/workplan-session-model';

export interface SaveSession$Params {
  sessionId: string;
      body?: WorkplanSessionModel
}

export function saveSession(http: HttpClient, rootUrl: string, params: SaveSession$Params, context?: HttpContext): Observable<StrictHttpResponse<WorkplanSessionModel>> {
  const rb = new RequestBuilder(rootUrl, saveSession.PATH, 'post');
  if (params) {
    rb.path('sessionId', params.sessionId, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WorkplanSessionModel>;
    })
  );
}

saveSession.PATH = '/api/moryx/workplans/sessions/{sessionId}/save';

