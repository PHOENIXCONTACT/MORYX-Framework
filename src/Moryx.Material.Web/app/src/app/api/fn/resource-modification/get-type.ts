/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResourceTypeModel } from '../../models/resource-type-model';

export interface GetType$Params {
  name: string;
}

export function getType(http: HttpClient, rootUrl: string, params: GetType$Params, context?: HttpContext): Observable<StrictHttpResponse<ResourceTypeModel>> {
  const rb = new RequestBuilder(rootUrl, getType.PATH, 'get');
  if (params) {
    rb.path('name', params.name, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResourceTypeModel>;
    })
  );
}

getType.PATH = '/api/moryx/resources/types/{name}';
