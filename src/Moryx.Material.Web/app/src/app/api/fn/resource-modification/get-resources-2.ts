/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResourceModel } from '../../models/resource-model';
import { ResourceQuery } from '../../models/resource-query';

export interface GetResources_2$Params {
      body?: ResourceQuery
}

export function getResources_2(http: HttpClient, rootUrl: string, params?: GetResources_2$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ResourceModel>>> {
  const rb = new RequestBuilder(rootUrl, getResources_2.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ResourceModel>>;
    })
  );
}

getResources_2.PATH = '/api/moryx/resources/query';
