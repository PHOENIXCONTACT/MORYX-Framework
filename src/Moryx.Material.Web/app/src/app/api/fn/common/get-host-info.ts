/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { HostInformationResponse } from '../../models/host-information-response';

export interface GetHostInfo$Params {
}

export function getHostInfo(http: HttpClient, rootUrl: string, params?: GetHostInfo$Params, context?: HttpContext): Observable<StrictHttpResponse<HostInformationResponse>> {
  const rb = new RequestBuilder(rootUrl, getHostInfo.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<HostInformationResponse>;
    })
  );
}

getHostInfo.PATH = '/common/info/system';
