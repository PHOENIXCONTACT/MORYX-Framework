/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Config } from '../../models/config';

export interface GetConfig$Json$Params {
  moduleName: string;
}

export function getConfig$Json(http: HttpClient, rootUrl: string, params: GetConfig$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<Config>> {
  const rb = new RequestBuilder(rootUrl, getConfig$Json.PATH, 'get');
  if (params) {
    rb.path('moduleName', params.moduleName, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Config>;
    })
  );
}

getConfig$Json.PATH = '/modules/{moduleName}/config';
