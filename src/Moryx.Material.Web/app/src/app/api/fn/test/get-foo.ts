/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Foo } from '../../models/foo';

export interface GetFoo$Params {
  value: number;
}

export function getFoo(http: HttpClient, rootUrl: string, params: GetFoo$Params, context?: HttpContext): Observable<StrictHttpResponse<Foo>> {
  const rb = new RequestBuilder(rootUrl, getFoo.PATH, 'get');
  if (params) {
    rb.path('value', params.value, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Foo>;
    })
  );
}

getFoo.PATH = '/foo/{value}';
