/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MaterialContainerModel } from '../../models/material-container-model';

export interface ContainerChanges$Params {
}

export function containerChanges(http: HttpClient, rootUrl: string, params?: ContainerChanges$Params, context?: HttpContext): Observable<StrictHttpResponse<MaterialContainerModel>> {
  const rb = new RequestBuilder(rootUrl, containerChanges.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MaterialContainerModel>;
    })
  );
}

containerChanges.PATH = '/api/moryx/materials/containers/stream';
