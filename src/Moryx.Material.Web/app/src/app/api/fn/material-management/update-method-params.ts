/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MethodEntry } from '../../models/method-entry';

export interface UpdateMethodParams$Params {
  type: string;
      body?: MethodEntry
}

export function updateMethodParams(http: HttpClient, rootUrl: string, params: UpdateMethodParams$Params, context?: HttpContext): Observable<StrictHttpResponse<MethodEntry>> {
  const rb = new RequestBuilder(rootUrl, updateMethodParams.PATH, 'post');
  if (params) {
    rb.path('type', params.type, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MethodEntry>;
    })
  );
}

updateMethodParams.PATH = '/api/moryx/materials/containers/update-method-params/{type}';
