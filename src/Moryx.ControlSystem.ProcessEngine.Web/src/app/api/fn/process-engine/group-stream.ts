/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProcessHolderGroupModel } from '../../models/process-holder-group-model';

export interface GroupStream$Params {
}

export function groupStream(http: HttpClient, rootUrl: string, params?: GroupStream$Params, context?: HttpContext): Observable<StrictHttpResponse<ProcessHolderGroupModel>> {
  const rb = new RequestBuilder(rootUrl, groupStream.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProcessHolderGroupModel>;
    })
  );
}

groupStream.PATH = '/api/moryx/processes/holders/stream';
