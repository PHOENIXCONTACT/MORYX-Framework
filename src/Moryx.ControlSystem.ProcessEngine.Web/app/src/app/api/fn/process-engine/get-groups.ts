/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProcessHolderGroupModelArrayApiResponse } from '../../models/process-holder-group-model-array-api-response';

export interface GetGroups$Params {
}

export function getGroups(http: HttpClient, rootUrl: string, params?: GetGroups$Params, context?: HttpContext): Observable<StrictHttpResponse<ProcessHolderGroupModelArrayApiResponse>> {
  const rb = new RequestBuilder(rootUrl, getGroups.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProcessHolderGroupModelArrayApiResponse>;
    })
  );
}

getGroups.PATH = '/api/moryx/processes/holders/groups';

