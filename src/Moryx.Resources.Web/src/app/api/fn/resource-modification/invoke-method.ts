/*
 * Copyright (c) 2026 Phoenix Contact GmbH & Co. KG
 * Licensed under the Apache License, Version 2.0
*/

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Entry } from '@moryx/ngx-web-framework/entry-editor';

export interface InvokeMethod$Params {
  id: number;
  method: string;
      body?: Entry
}

export function invokeMethod(http: HttpClient, rootUrl: string, params: InvokeMethod$Params, context?: HttpContext): Observable<StrictHttpResponse<Entry>> {
  const rb = new RequestBuilder(rootUrl, invokeMethod.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.path('method', params.method, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Entry>;
    })
  );
}

invokeMethod.PATH = '/api/moryx/resources/{id}/invoke/{method}';

