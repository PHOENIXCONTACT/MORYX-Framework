/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProductModel as MoryxAbstractionLayerProductsEndpointsProductModel } from '../../models/Moryx/AbstractionLayer/Products/Endpoints/product-model';
import { Entry as MoryxSerializationEntry } from '../../models/Moryx/Serialization/entry';

export interface Import$Params {
  importerName: string;
      body?: MoryxSerializationEntry
}

export function import$(http: HttpClient, rootUrl: string, params: Import$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<MoryxAbstractionLayerProductsEndpointsProductModel>>> {
  const rb = new RequestBuilder(rootUrl, import$.PATH, 'post');
  if (params) {
    rb.path('importerName', params.importerName, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<MoryxAbstractionLayerProductsEndpointsProductModel>>;
    })
  );
}

import$.PATH = '/api/moryx/products/importers/{importerName}';
