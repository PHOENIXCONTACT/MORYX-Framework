/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProductModel as MoryxAbstractionLayerProductsEndpointsProductModel } from '../../models/Moryx/AbstractionLayer/Products/Endpoints/product-model';

export interface Duplicate$Params {
  id: number;
      body?: string
}

export function duplicate(http: HttpClient, rootUrl: string, params: Duplicate$Params, context?: HttpContext): Observable<StrictHttpResponse<MoryxAbstractionLayerProductsEndpointsProductModel>> {
  const rb = new RequestBuilder(rootUrl, duplicate.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MoryxAbstractionLayerProductsEndpointsProductModel>;
    })
  );
}

duplicate.PATH = '/api/moryx/products/types/{id}';
