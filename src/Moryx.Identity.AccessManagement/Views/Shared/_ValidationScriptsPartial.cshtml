@* Client-side validation has been removed with jQuery migration *@
@* ASP.NET Core ModelState validation still works server-side *@
@* If client-side validation is needed in the future, consider using vanilla JS validation or aspnet-validation library *@
