{
  "name": "components",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build --configuration production && copyfiles -u 1 dist/*.js ../wwwroot/js",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  }
}
