/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { InstructionModel } from '../../models/instruction-model';

export interface InstructionStream$Params {
}

export function instructionStream(http: HttpClient, rootUrl: string, params?: InstructionStream$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<InstructionModel>>> {
  const rb = new RequestBuilder(rootUrl, instructionStream.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<InstructionModel>>;
    })
  );
}

instructionStream.PATH = '/api/moryx/instructions/stream';
