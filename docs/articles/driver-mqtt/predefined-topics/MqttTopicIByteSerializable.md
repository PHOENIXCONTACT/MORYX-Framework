# MqttTopicIByteSerializable

The byte serializable topic supports all message types that define their own conversion to and from bytes using the IByteSerializable interface with the ToBytes and FromBytes methods.
